# ------------------------------------------------------------------------------
# AUTHENTICATION (NextAuth.js)
# ------------------------------------------------------------------------------

# The canonical URL of your site.
# Localhost: http://localhost:3000
# Production: https://your-domain.com
NEXTAUTH_URL="http://localhost:3000"

# A random string used to hash tokens and sign session cookies.
# CRITICAL: This must be set in production to avoid build crashes and security risks.
# Generate one via terminal: openssl rand -base64 32
NEXTAUTH_SECRET=""

# ------------------------------------------------------------------------------
# WALLET CONNECT (RainbowKit)
# ------------------------------------------------------------------------------

# Required for WalletConnect v2 support (QR code connections).
# Get one for free at: https://cloud.walletconnect.com/
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=""

# ------------------------------------------------------------------------------
# BLOCKCHAIN PROVIDER (Wagmi & Viem)
# ------------------------------------------------------------------------------

# RPC URL for connecting to the blockchain (Base Mainnet).
# Recommended: Use a paid/free provider like Alchemy or Infura to avoid rate limits.
# Example: https://base-mainnet.g.alchemy.com/v2/your-api-key

# Client-side (Public)
NEXT_PUBLIC_ALCHEMY_HTTP_URL="https://base-mainnet.g.alchemy.com/v2/your-api-key"

# Server-side (Private - optional if same as public, but good practice to separate)
ALCHEMY_HTTP_URL="https://base-mainnet.g.alchemy.com/v2/your-api-key"

# ------------------------------------------------------------------------------
# TOKEN GATING
# ------------------------------------------------------------------------------

# The address of the NFT contract to check ownership against.
TOKEN_GATE_CONTRACT="0x..."

# The type of contract (e.g., erc721, erc1155).
TOKEN_GATE_TYPE="erc721"

# The Chain ID where the contract is deployed (e.g., 8453 for Base).
TOKEN_GATE_CHAIN_ID="8453"

# Optional: Comma-separated list of token IDs to blacklist.
# TOKEN_GATE_INVALID_IDS="1,2,3"

# ------------------------------------------------------------------------------
# EXTERNAL APIS
# ------------------------------------------------------------------------------

# CoinGecko API Key (Optional, but recommended to avoid rate limits).
CG_API_KEY=""

# ------------------------------------------------------------------------------
# APP CONFIG
# ------------------------------------------------------------------------------

# The name of the application displayed in wallet connection modals.
NEXT_PUBLIC_APP_NAME="Token-Gated Price Checker"